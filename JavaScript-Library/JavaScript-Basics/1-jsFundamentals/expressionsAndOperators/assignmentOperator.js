/*
The assignment operator is used to assign values using the initialization 
(right habd sude if tge =) to supply the declaration (left side of the =) a value.
*/

let r=5
//   ^
//assignment operator